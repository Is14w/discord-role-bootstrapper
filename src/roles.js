/**
 * This list is created only when you explicitly run create-roles with a server ID.
 * Existing roles with the same name are left unchanged. Use Unicode escapes for
 * non-ASCII role names to keep this source file portable across Windows shells.
 */
module.exports = [
  {
    name: "\u7ba1\u7406\u5458",
    colors: { primaryColor: "#e74c3c" },
    hoist: true,
    mentionable: false,
    reason: "Initial server role setup"
  }
];
